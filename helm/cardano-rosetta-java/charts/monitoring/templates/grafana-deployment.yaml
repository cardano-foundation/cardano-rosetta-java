apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "monitoring.grafanaName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "monitoring.commonLabels" . | nindent 4 }}
    component: grafana
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ include "monitoring.grafanaName" . }}
  template:
    metadata:
      labels:
        app: {{ include "monitoring.grafanaName" . }}
        component: grafana
    spec:
      securityContext:
        fsGroup: 472
        runAsUser: 472
      containers:
        - name: grafana
          image: {{ .Values.grafana.image | quote }}
          env:
            - name: GF_SECURITY_ADMIN_USER
              valueFrom:
                secretKeyRef:
                  name: {{ printf "%s-grafana-secret" .Release.Name | trunc 63 | trimSuffix "-" }}
                  key: admin-user
            - name: GF_SECURITY_ADMIN_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ printf "%s-grafana-secret" .Release.Name | trunc 63 | trimSuffix "-" }}
                  key: admin-password
            - name: GF_PATHS_PROVISIONING
              value: /etc/grafana/provisioning
          ports:
            - name: http
              containerPort: 3000
              protocol: TCP
          volumeMounts:
            - name: grafana-data
              mountPath: /var/lib/grafana
            - name: datasources
              mountPath: /etc/grafana/provisioning/datasources
            - name: dashboard-provider
              mountPath: /etc/grafana/provisioning/dashboards
            - name: dashboard-json
              mountPath: /etc/grafana/provisioning/dashboards/json
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          readinessProbe:
            httpGet:
              path: /api/health
              port: 3000
            initialDelaySeconds: 20
            periodSeconds: 10
            failureThreshold: 10
          livenessProbe:
            httpGet:
              path: /api/health
              port: 3000
            initialDelaySeconds: 60
            periodSeconds: 30
            failureThreshold: 5
      volumes:
        - name: grafana-data
          persistentVolumeClaim:
            claimName: {{ printf "%s-grafana-data" .Release.Name | trunc 63 | trimSuffix "-" }}
        - name: datasources
          configMap:
            name: {{ printf "%s-grafana-datasources" .Release.Name | trunc 63 | trimSuffix "-" }}
        - name: dashboard-provider
          configMap:
            name: {{ printf "%s-grafana-dashboard-provider" .Release.Name | trunc 63 | trimSuffix "-" }}
        - name: dashboard-json
          configMap:
            name: {{ printf "%s-grafana-dashboards" .Release.Name | trunc 63 | trimSuffix "-" }}
