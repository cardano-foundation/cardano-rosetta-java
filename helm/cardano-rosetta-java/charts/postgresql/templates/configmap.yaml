apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ printf "%s-postgresql-config" .Release.Name }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "postgresql.commonLabels" . | nindent 4 }}
data:
  NETWORK: {{ .Values.global.network | quote }}
