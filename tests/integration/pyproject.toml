[project]
name = "rosetta-validator"
version = "0.1.0"
description = "Rosetta construction endpoint validator"
requires-python = ">=3.10"
dependencies = [
    "jsonschema>=4.25.1",
    "pyyaml>=6.0.2",
]
