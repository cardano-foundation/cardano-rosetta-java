{
  "test_name": "10 inputs → 10 outputs with byron addresses hash",
  "description": "Hash test for Payloads for 10 inputs → 10 outputs with byron addresses (skipped due to combine error)",
  "request_body": {
    "network_identifier": {
      "blockchain": "cardano",
      "network": "{{networkId}}"
    },
    "signed_transaction": ""
  }
}