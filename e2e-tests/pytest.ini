[pytest]
# Primary pytest configuration file
# Path and discovery settings are in pyproject.toml

# Execution settings
addopts = --no-header --tb=short --no-summary

# Asyncio settings
asyncio_mode = strict
asyncio_default_fixture_loop_scope = function 