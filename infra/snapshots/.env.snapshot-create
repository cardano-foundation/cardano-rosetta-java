## Database Snapshot Creation Configuration
## This file contains ONLY variables specific to snapshot creation
## Use alongside .env.docker-compose-preprod to avoid duplication
## Usage: docker-compose --env-file .env.docker-compose-preprod --env-file .env.snapshot-create ...

## Snapshot Creation Specific Configuration
SNAPSHOT_DESTINATION_PATH=/snapshots
# Directory inside container where snapshots are created
SNAPSHOT_RETENTION_DAYS=30
# Number of days to retain old snapshots (0 = disable cleanup)

## IAGON Upload Configuration (CREATE specific)
IAGON_UPLOAD_ENABLED=true
# Enable upload to IAGON cloud storage
IAGON_API_KEY=
# IAGON API key for authentication (REQUIRED if upload enabled)
# Example: IAGON_API_KEY="ll.IAG-SN.xxxxxx.xxxxxx.xxxxxx"
IAGON_GATEWAY=https://gw.iagon.com
# IAGON API gateway endpoint
IAGON_CHUNK_SIZE_MB=190
# Chunk size in MB for large file uploads