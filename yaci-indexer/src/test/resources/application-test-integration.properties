store.cardano.protocol-magic=${PROTOCOL_MAGIC:42}

store.cardano.byron-genesis-file=${GENESIS_BYRON_PATH:../config/node/devkit/byron-genesis.json}
store.cardano.shelley-genesis-file=${GENESIS_SHELLEY_PATH:../config/node/devkit/shelley-genesis.json}
store.cardano.alonzo-genesis-file=${GENESIS_ALONZO_PATH:../config/node/devkit/alonzo-genesis.json}
store.cardano.conway-genesis-file=${GENESIS_CONWAY_PATH:../config/node/devkit/conway-genesis.json}

spring.datasource.url=jdbc:h2:file:../testData/devkit.db
spring.datasource.username=${DB_USER:rosetta_db_admin}
spring.datasource.password=${DB_SECRET:weakpwd#123_d}
spring.h2.console.enabled=true

store.cardano.host=${CARDANO_NODE_HOST:localhost}
store.cardano.port=${CARDANO_NODE_PORT:8090}
store.cardano.return-tx-body-cbor=true

store.utxo.pruning-interval=3600
store.utxo.pruning-safe-blocks=${REMOVE_SPENT_UTXOS_LAST_BLOCKS_GRACE_COUNT:129600}
store.utxo.pruning-batch-size=${REMOVE_SPENT_UTXOS_BATCH_SIZE:3000}